import React from 'react';
import '../../scss/utils/_tags.scss'

const Tags = (props) => {
      return (
            <div>
                  <span>{props.tags}</span>
            </div>
      );
};

export default Tags;
